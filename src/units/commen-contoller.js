// const express = require('express');

// const router = express.Router();

// // Common GET handler
// router.get('/:id', (req, res) => {
//     const id = req.params.id;
//     // Your logic to handle GET request
//     res.send(`GET request received for ID: ${id}`);
// });

// // Common POST handler
// router.post('/', (req, res) => {
//     const data = req.body;
//     // Your logic to handle POST request
//     res.send('POST request received with data: ' + JSON.stringify(data));
// });

// // Common PUT handler
// router.put('/:id', (req, res) => {
//     const id = req.params.id;
//     const data = req.body;
//     // Your logic to handle PUT request
//     res.send(`PUT request received for ID: ${id} with data: ` + JSON.stringify(data));
// });

// // Common DELETE handler
// router.delete('/:id', (req, res) => {
//     const id = req.params.id;
//     // Your logic to handle DELETE request
//     res.send(`DELETE request received for ID: ${id}`);
// });

// module.exports = router;